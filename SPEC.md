# 蔵書スキャナ — 仕様書 / Claude Code 用プロンプト

> このファイルをリポジトリ直下に `SPEC.md` として置き、Claude Code で
> 「`SPEC.md` を読んで、フェーズ1から実装して」と指示する。
> 一度に全部作らせず、フェーズごとに実機（iPhone Safari）で確認しながら進める。

---

## 0. 目的

自宅にある**購入済みの蔵書**を、iPhone のカメラで背表紙裏のバーコード（ISBN）を
連続スキャンして記録し、書名・著者・出版社を自動取得してデータベース化する。
最終的に Word 形式（.docx）の蔵書目録として書き出し、iOS の共有シート経由で
Google ドライブに保存する。

外部サーバーは一切持たない。GitHub Pages で配信する静的アプリとして完結させる。

---

## 1. 技術構成（決定事項）

| 項目 | 採用 | 理由 |
|---|---|---|
| ホスティング | GitHub Pages（HTTPS） | `getUserMedia` は HTTPS 必須。ビルド不要で運用できる |
| バーコード読取 | **zxing-wasm** | `BarcodeDetector` は Safari / iOS 全ブラウザで未実装のため使用不可。純 JS の `@zxing/browser` より WASM の方が実機で明確に速い |
| 書誌データ | **openBD** →（404時）**Google Books API** | どちらも API キー不要・CORS 許可済み。Amazon PA-API は署名鍵が必要でブラウザに置けず、CORS も通らないため採用しない |
| 保存 | IndexedDB | 件数が増えても耐える。localStorage は使わない |
| Word 出力 | `docx` ライブラリ（UMD, CDN） | ブラウザ単体で正規の .docx を生成できる |
| ドライブ送信 | Web Share API (`navigator.share` with files) | OAuth 不要。共有シートから Google ドライブアプリへ渡せる |
| 形態 | 単一 HTML + CDN からの ESM/UMD 読み込み、PWA 化（manifest + Service Worker） | |

**依存の読み込み方針**：CDN は jsDelivr または esm.sh を使い、**バージョンを必ず固定**する
（`@x.y.z` を明示）。ライブラリの正確なエクスポート名・シグネチャは、こちらの記憶に頼らず
**固定したバージョンの公式ドキュメントを確認してから実装すること**。特に zxing-wasm は
バージョン間で API 名が変わっている。

---

## 2. データモデル

IndexedDB: DB名 `bookshelf` / object store `books` / keyPath `isbn13`

```
{
  isbn13:     "9784XXXXXXXXX",  // 主キー。重複登録はここで弾く
  title:      "",
  subtitle:   "",
  authors:    "",               // 複数は "／" 区切りで連結
  publisher:  "",
  pubdate:    "",               // "2024-03" など取得できた粒度のまま
  coverUrl:   "",               // 書影URL（表示用。保存は任意）
  location:   "",               // 「本棚A 3段目」等、自由入力
  tags:       [],
  note:       "",
  source:     "openbd" | "googlebooks" | "manual",
  scannedAt:  "ISO8601"
}
```

---

## 3. 機能要件

### 3-1. スキャン画面

- リアカメラのライブプレビューを表示し、連続でスキャンし続ける
- **`978` / `979` で始まる EAN-13 のみ採用する**
  （日本の書籍は2段バーコードで、下段は `192...` の分類・価格コード。これを拾うと誤登録になる）
- EAN-13 のチェックディジットを検証してから採用
- 読取成功時：短いビープ＋バイブレーション、画面上部に書名をトースト表示
- 直近に読んだ ISBN は数秒間クールダウンして二重読取を防ぐ
- **既に登録済みの ISBN を読んだら「登録済み」と明示**して、上書きせずスキップ
  （蔵書の重複購入チェックとしても機能させる）
- スキャンを止めずに次々読めること。書誌取得は非同期で裏に流し、UI を止めない

### 3-2. 手動入力

バーコードのない古い本・非売品・洋書のために、ISBN 手入力および
全項目手入力での登録を必ず用意する。これがないと実用にならない。

### 3-3. 一覧画面

- カード or テーブル表示（書影があれば表示）
- 書名・著者・出版社での絞り込み検索
- ソート：登録日 / 書名 / 著者 / 出版社
- 各レコードの編集・削除
- 総冊数の表示

### 3-4. Word 出力

`docx` ライブラリで .docx を生成する。

- 1ページ目に「蔵書目録」見出し、出力日時、総冊数
- 本体は表：`No. / 書名 / 著者 / 出版社 / 出版年 / ISBN / 配置場所`
- 表のヘッダ行は繰り返し設定（`tableHeader: true`）
- 出力前に、現在の絞り込み結果だけを出すか全件を出すかを選ばせる

### 3-5. 共有 / バックアップ

- `navigator.canShare({files:[file]})` で判定し、可なら `navigator.share`
  → 不可なら通常のダウンロードにフォールバック
- 共有はユーザーのタップ起点で呼ぶこと（非同期処理を挟むと iOS で失敗する場合があるため、
  Blob 生成を待ってから share を呼ぶ構造に注意）
- **JSON での全件エクスポート／インポートを必ず実装する**（後述の理由により必須）

---

## 4. iOS 固有の注意点（実装時に必ず守る）

1. `<video>` に `playsinline muted autoplay` を付ける。`playsinline` がないと全画面に乗っ取られる
2. カメラは `{ video: { facingMode: { ideal: "environment" } } }`
3. カメラ起動はユーザーのタップ起点で行う（自動起動しない）
4. 画面を離れる／タブが非アクティブになったら `track.stop()` でカメラを確実に解放する
5. スキャン中は `screen.wakeLock` を試みる（未対応なら無視）
6. **IndexedDB は iOS Safari の ITP により、未使用期間が続くと消される可能性がある。**
   ホーム画面に追加した PWA は対象外になるため、初回起動時に
   「ホーム画面に追加して使ってください」と案内を出す。
   併せて JSON エクスポートによる定期バックアップを促す UI を置く

---

## 5. 実装フェーズ

各フェーズが実機で動作確認できた時点で git commit すること。次に進む前に必ず止まって報告。

**フェーズ1 — カメラとバーコード読取だけ**
画面に読み取れた ISBN の数字を出すだけのものを作る。書誌取得も保存もしない。
ここが一番ハマるので、単独で完成させる。`978`/`979` フィルタとチェックディジット検証まで。

**フェーズ2 — 書誌取得**
openBD → Google Books のフォールバック実装。両方とも見つからない場合は
「ISBN のみ登録（書名は手入力してください）」として扱う。

**フェーズ3 — IndexedDB と一覧画面**
保存・重複判定・検索・編集・削除。JSON エクスポート/インポートもここで入れる。

**フェーズ4 — Word 出力**

**フェーズ5 — 共有と PWA 化**
manifest.json、アイコン、Service Worker（CDN 資産のキャッシュ）、Web Share。

---

## 6. スコープ外（今回はやらない）

- Amazon 連携、価格取得、相場チェック
- Google Drive API による直接アップロード（まずは共有シートで運用し、
  不便なら後から OAuth 版を検討する）
- 複数端末同期、クラウド保存
- ログイン機能

---

## 7. コーディング上の要求

- コメントは日本語
- 外部ライブラリのバージョンは固定し、`README.md` に依存一覧を記載する
- エラーは握り潰さず、画面に何が起きたか出す（特にカメラ権限拒否時とネットワークエラー時）
- 通信失敗しても既に登録済みのデータが壊れないこと
- 最初から凝った UI にしない。片手で持って親指で操作できることを最優先にする
